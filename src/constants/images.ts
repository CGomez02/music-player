import unknoknArtistImage from '@/assets/unknown_artist.png'
import unknoknTrackImage from '@/assets/unknown_track.png'
import { Image } from 'react-native'

export const unknownTrackImgeUri = Image.resolveAssetSource(unknoknTrackImage).uri
export const unknownArtistImgeUri = Image.resolveAssetSource(unknoknArtistImage).uri

